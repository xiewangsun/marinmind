# 后续优化方向

> 记录已知的 v1 限制与计划中的优化项，随版本推进消化。

## 阅读视图（v1 遗留）

- [ ] **文字摘录**：透明 textLayer（pdf.js `getTextContent`）+ 划选文字 → text 卡片，
      高亮矩形从文本项精确计算（当前只有区域框选）。
- [ ] **多标签同步**：同一 PDF 开多个阅读标签时，高亮各自持有快照互不同步；
      可引入事件总线或定期 `listByDocument` 刷新。
- [ ] **混合页尺寸**：当前统一按第 1 页宽 fit；混合尺寸 PDF 可按页自适应。
- [ ] **摘录颜色/样式**：多种高亮颜色、透明度设置（数据层 `Card.color` 字段已预留）。
- [ ] **页码跳转 / 目录（PDF outline）导航栏**。
- [ ] **滚动位置记忆**：视图状态（当前页、缩放）存入 ViewState，
      重开时恢复（当前重开回到第一页 + 适应宽度）。
- [ ] **Esc 取消拖拽**：当前仅右键取消。
- [ ] **移动端验证**：`loadPdfJs` 移动端可用，但 canvas 内存压力大，
      dpr/像素钳制与懒卸载已做，需真机验证。

## 数据层

- [ ] 卡片批量操作接口（多选删除/移动文档）。
- [ ] `.marginpkg` 风格备份导出/导入（SQLite + 文档打包）。
- [ ] 数据库文件健康检查与损坏恢复（打开失败时备份旧文件并重建）。

## 复习/算法

- [ ] SM-2 → FSRS 升级评估（`ReviewState` 字段已按可替换设计）。
- [ ] 复习界面（闪卡四档评分、回链原文跳转）。

## 工程

- [ ] ESLint/Prettier 接入。
- [ ] 视图层测试策略（jsdom 或 Playwright 冒烟）。
- [ ] GitHub Actions：构建 + 测试 + 发版（versions.json 版本同步）。
